version: 2.1

defaults: &defaults
  docker:
    - image: circleci/clojure:tools-deps-1.9.0.381

orbs:
  cache: compute/cache@0.0.3

jobs:
  test:
    <<: *defaults

    steps:
      - checkout
      - run: clojure -A:test

workflows:
  version: 2
  ci-workflow:
    jobs:
      - test